concepts :
	-	URI URL URN	:	 done
	-	socket programming tutorial
	-	what is http
	-	record boundaries sockstream

	-	book
	-	conf file

	-	set an error mechanism

	-	check handle request
		-	reparse the request
		-	check request validity
		-	handle chunked requests


--------------------------------------------------------------------------------
-	host in the same server witch has the same name
-	get : path is symbolic link ??

-	add Server header field
# set the header part max length


-	add timeout to the connection
-	default host port = 8080 : done
-	check select fd > fd_maxset : done
-	http version should not excedd 1.1 : done
-	add no-blocking to the sockets : done
-	set request line max size
-	ignore sigpipe : done


parsing:
	-	request line:
		-	method : done
		-	uri :
			-	scheme :
			-	authority :
				-	user info :
				-	host :
				-	port : done
			-	path
			-	query : done
			-	fragment : done
		-	version : done
	-	headers:
		-	key :
		-	value :
--------------------------------------------------------------------------------

location	/www/var
{
	root	/usr
	cgi		php		/usr/bin/php-cgi
}


uri /www/var/bin/php-cgi/exrta1/extra2  -->  /usr/bin/php-cgi/exrta1/extra2

scrpit name : /www/var/bin/php-cgi
path info : /exrta1/extra2
path translated : find path info f ga3 location

request uri : subpath/exrta1/extra2
